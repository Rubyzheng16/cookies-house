<!--碎片收纳主界面-->
<view class="page-root container">
  <!-- 头部 -->
  <view class="header">
    <view class="header-content">
      <view class="calendar-btn" bindtap="showCalendar">
        <text class="calendar-icon">📅</text>
      </view>
      <text class="header-title">情绪饼干屋</text>
      <view class="edit-btn {{isEditMode ? 'edit-active' : ''}}" bindtap="toggleEditMode">
        <text class="edit-btn-text">{{isEditMode ? '完成' : '编辑'}}</text>
      </view>
    </view>
  </view>

  <!-- 日历弹窗 -->
  <view class="calendar-modal" wx:if="{{showCalendarModal}}" bindtap="hideCalendar">
    <view class="calendar-content" catchtap="stopPropagation">
      <view class="calendar-header">
        <view class="calendar-header-left">
          <view class="calendar-nav-btn" bindtap="prevYear">‹‹</view>
          <view class="calendar-nav-btn" bindtap="prevMonth">‹</view>
          <text class="calendar-title">{{currentMonth}}</text>
          <view class="calendar-nav-btn" bindtap="nextMonth">›</view>
          <view class="calendar-nav-btn" bindtap="nextYear">››</view>
        </view>
        <text class="calendar-close" bindtap="hideCalendar">✕</text>
      </view>
      <view class="calendar-grid">
        <view class="calendar-weekdays">
          <text wx:for="{{['日','一','二','三','四','五','六']}}" wx:key="*this" class="weekday">{{item}}</text>
        </view>
        <view class="calendar-days">
          <view 
            wx:for="{{calendarDays}}" 
            wx:key="index"
            class="calendar-day {{item.isCurrentMonth ? '' : 'other-month'}} {{item.isToday ? 'today' : ''}} {{item.date === selectedDate ? 'selected' : ''}}"
            bindtap="selectCalendarDate"
            data-date="{{item.date}}"
          >
            <text class="day-number">{{item.day}}</text>
            <view wx:if="{{item.hasContent}}" class="day-dot"></view>
          </view>
        </view>
      </view>
    </view>
  </view>

  <!-- 文件夹网格 -->
  <view class="folders-grid">
    <view 
      wx:for="{{folders}}" 
      wx:key="date"
      class="folder-card"
    >
      <view class="folder-content" bindtap="openFolder" data-date="{{item.date}}">
        <text class="folder-icon">📂</text>
        <text class="folder-date-label">{{item.date}}</text>
      </view>
      <view wx:if="{{isEditMode}}" class="folder-delete-btn" catchtap="deleteFolder" data-date="{{item.date}}">
        <text class="delete-icon">×</text>
      </view>
    </view>
  </view>

  <!-- 输入弹窗 -->
  <input-modal 
    show="{{showInputModal}}"
    bind:close="hideInputModal"
    bind:confirm="handleInputConfirm"
  />
</view>
